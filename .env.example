# Optional WKD publishing. Leave both empty to disable it (the theme default).
# Use your email and a repository-relative public key file, then rebuild.
# Example: WKD_EMAIL=hello@example.org
# Example: WKD_PUBLIC_KEY_PATH=public/assets/pgp/public-key.asc
WKD_EMAIL=
WKD_PUBLIC_KEY_PATH=
